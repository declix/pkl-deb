/// Simple tests for SourcesList module
amends "pkl:test"

import "../SourcesList.pkl"

examples {
    ["Basic Debian sources.list"] {
        new SourcesList.Entry {
            archiveType = "deb"
            uri = "http://deb.debian.org/debian"
            suite = "bookworm"
            components = new Listing { "main" }
        }
    }
}